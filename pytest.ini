[pytest]
markers =
    unit: deterministic pure-logic tests safe for default CI.
    integration: integration tests requiring optional components or external resources.
    model: tests that require a loaded LLM model.
    eval: retrieval/quality evaluation tests.
    server: tests that require a running AndesCode server endpoint.
addopts = -m "not integration and not model and not server and not eval"
