# AndesCode — Python dependencies
# Install with: pip3 install -r requirements.txt

# ── Core server ───────────────────────────────────────────────────────────────
fastapi>=0.115.0              # web framework, routing, StaticFiles
uvicorn>=0.30.0               # ASGI server
aiofiles>=23.0.0              # async static file serving (required by FastAPI StaticFiles)
python-dotenv>=1.0.0          # .env config loading

# ── LLM inference ─────────────────────────────────────────────────────────────
llama-cpp-python>=0.3.0       # llama.cpp Python bindings — local model inference

# ── Codebase indexer ──────────────────────────────────────────────────────────
chromadb>=0.5.0               # local vector database
sentence-transformers>=3.0.0  # code embedding model (all-MiniLM-L6-v2)

# ── Model download (one-time) ─────────────────────────────────────────────────
huggingface_hub>=1.0.0        # download Gemma 4 GGUF from Hugging Face
certifi>=2024.0.0
